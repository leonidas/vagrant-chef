# Setup

1. Install ruby (1.9.3 or 2.0)
2. Install VirtualBox
3. Install Vagrant

```
git clone git@github.com:leonidas/vagrant-chef
gem install chef
vagrant plugin install vagrant-omnibus
vagrant plugin install vagrant-berkshelf
vagrant up
```

